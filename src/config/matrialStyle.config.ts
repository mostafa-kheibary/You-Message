import { createTheme } from '@mui/material';

// configure material styles
const defultTheme = createTheme({
  typography: { fontFamily: 'Poppins, sans-serif' },
  palette: { primary: { main: '#1289FF' } },
});
export default defultTheme;
